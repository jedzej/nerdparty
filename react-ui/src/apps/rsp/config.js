import MANIFEST from './manifest'
import MAIN from './containers/RspApp'
import CARD from './containers/RspCard'
import epic from './epics'

const config = {
  MANIFEST,
  MAIN,
  CARD,
  epic
}

export default config;
